{
  "name": "audio-tempo-webapp",
  "version": "1.0.0",
  "scripts": {
    "start": "apt-get update -qq && apt-get install -y -qq ffmpeg python3-pip && pip3 install aubio -q && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "ejs": "^3.1.9"
  }
}
